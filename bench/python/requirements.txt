# Benchmark-only dependencies for the Python harness.
# `xprec` is only published as an sdist, so pip builds the C extension.
numpy>=1.24
xprec>=99.0.0   # TEMPORARY: unsatisfiable, to make the Python harness fail
